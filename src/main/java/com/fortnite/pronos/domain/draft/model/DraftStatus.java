package com.fortnite.pronos.domain.draft.model;

/** Draft lifecycle states for the pure Draft domain model. */
public enum DraftStatus {
  PENDING,
  ACTIVE,
  IN_PROGRESS,
  PAUSED,
  FINISHED,
  CANCELLED
}
