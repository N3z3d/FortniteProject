JIRA BACKLOG - FORTNITE PRONOS
Last Updated: 2026-02-08 12:06
Source of Truth: Jira-tache.txt

================================================================================
RULES
================================================================================
- Fichier unique: Jira-tache.txt (source de verite, jamais supprime).
- Supprimer les tickets DONE du fichier.
- Attribution obligatoire avant travail (ASSIGNE + dates + STATUT).
- READY = Objectif, Contexte, Description, Criteres_d_acceptation.
- WIP max 2 tickets IN_PROGRESS par dev.
- Statuts autorises: TODO | IN_PROGRESS | BLOCKED | DONE.
- Template obligatoire ci-dessous pour chaque ticket.

================================================================================
TICKETS
================================================================================

ID: JIRA-FIX-002
TITRE: Audit complet traductions non cablees (toutes langues)
TYPE: Task
PRIORITE: P2
STATUT: TODO
ESTIMATE: 1-2j

ASSIGNE: ->
ASSIGNE_LE: ->
DERNIERE_MAJ: ->

DEPENDENCIES: none

OBJECTIF:
- Identifier et corriger toutes les cles i18n brutes restantes dans l'application.
- Harmoniser les traductions `en/fr/es/pt` au-dela du seul ecran rejoindre.

CONTEXTE:
- Le flow rejoindre est traite par `JIRA-FIX-001`, mais d'autres ecrans peuvent afficher des placeholders non traduits.
- Besoin d'un audit systematique sur l'ensemble des templates.

DESCRIPTION:
- Scanner tous les templates front pour detecter les textes `xxx.yyy.zzz` visibles et cles absentes des fichiers i18n.
- Completer les fichiers i18n `en/fr/es/pt` pour les ecrans critiques (navigation, games, profil, trades).
- Ajouter/mettre a jour des specs de non-regression i18n sur les zones corrigees.

CRITERES_D_ACCEPTATION:
- [ ] Aucune cle i18n brute visible sur les parcours principaux.
- [ ] Parite des cles corrigees entre `en/fr/es/pt`.
- [ ] Tests frontend cibles i18n verts.

NEXT:
- (1) Generer l'inventaire des cles manquantes/hors-cablage sur ecrans principaux
- (2) Corriger les fichiers i18n et les bindings templates correspondants
- (3) Executer specs i18n cibles + smoke frontend


ID: JIRA-FIX-003
TITRE: Endpoint mismatch join-with-code (404 ou erreur)
TYPE: Bug
PRIORITE: P1
STATUT: IN_PROGRESS
ESTIMATE: 2-4h

ASSIGNE: @Codex
ASSIGNE_LE: 2026-02-07 21:40
DERNIERE_MAJ: 2026-02-08 11:40

DEPENDENCIES: none

OBJECTIF:
- Corriger le mismatch entre l'URL appelee par le frontend pour rejoindre une partie avec code et l'endpoint backend correspondant.

CONTEXTE:
- Le flow "rejoindre une partie avec un code d'invitation" echoue (404 ou erreur inattendue).
- Cause probable: le frontend appelle un chemin different de celui expose par le controller backend.
- Le flow Thibaut -> Teddy (invitation) est bloque a cette etape.

DESCRIPTION:
- Tracer l'URL appelee par le service Angular (game.service.ts ou equivalent) lors du join-with-code.
- Comparer avec l'endpoint expose dans le GameController backend.
- Aligner le path frontend sur le path backend (ou inversement, selon la convention du projet).
- Tester le flow complet join-with-code de bout en bout.
- Avancement 2026-02-07 21:40: ajout endpoint backend `POST /api/games/join-with-code` compatible payload frontend `{ code: ... }`.
- Avancement 2026-02-07 21:40: endpoint normalise le code, valide l'utilisateur, execute `joinGame` et renvoie `GameDto`.
- Avancement 2026-02-07 21:40: tests verts backend (`GameControllerSimpleTest`) + frontend cible (`game-command.service.spec.ts`).
- Avancement 2026-02-07 22:52: frontend `joinGameWithCode` force `trim().toUpperCase()` avant envoi pour eviter les rejets de code.
- Avancement 2026-02-07 22:52: `join-game.component` affiche maintenant le message d'erreur backend detaille (au lieu d'un message generique).
- Avancement 2026-02-07 22:52: suites vertes `frontend game specs (317/317)` + backend `GameControllerSimpleTest,CreateGameUseCaseTest,JoinGameUseCaseTest`.
- Avancement 2026-02-07 23:31: backend relance localement et creation game revalidee via API (`POST /api/games`) avec code d'invitation retourne.
- Avancement 2026-02-07 23:31: feedback utilisateur conserve les messages backend detailles cote frontend (`game-command.service`).
- Avancement 2026-02-08 11:40: retour utilisateur confirme que le join-with-code fonctionne depuis le panneau lateral gauche.
- Avancement 2026-02-08 11:40: nouveau ecart detecte, le CTA "rejoindre avec un code" depuis la page Mes parties ne declenche pas le meme flow.

CRITERES_D_ACCEPTATION:
- [ ] Le frontend appelle le bon endpoint backend pour join-with-code.
- [ ] Le flow rejoindre avec code fonctionne de bout en bout.
- [ ] Tests frontend/backend cibles verts.

NEXT:
- (1) Tracer le clic "rejoindre avec un code" depuis la page Mes parties et comparer avec le flow sidebar
- (2) Aligner les deux points d'entree sur la meme methode `joinGameWithCode` + meme gestion d'erreurs
- (3) Revalider en manuel Thibaut -> Teddy depuis les deux emplacements UI


ID: JIRA-FIX-004
TITRE: Code invitation visible et partageable pour le createur
TYPE: Bug
PRIORITE: P1
STATUT: BLOCKED
ESTIMATE: 4-8h

ASSIGNE: @Codex
ASSIGNE_LE: 2026-02-07 21:12
DERNIERE_MAJ: 2026-02-08 11:54

DEPENDENCIES: JIRA-FIX-007

OBJECTIF:
- Garantir que le createur d'une partie peut voir et partager le code d'invitation.
- Rendre deterministe le cycle creation -> stockage -> lecture -> affichage du code.

CONTEXTE:
- Incoherence constatee: certaines parties ont un code visible, d'autres non.
- Le flow d'invitation est bloque si le createur ne peut pas transmettre le code.
- Le code doit etre visible dans le detail de la partie et copiable.

DESCRIPTION:
- Auditer les chemins `createGame`, `regenerateInvitationCode`, query game et mappings DTO.
- Uniformiser la regle: code genere et persiste systematiquement a la creation.
- Verifier la visibilite du code dans la reponse API et dans le composant frontend (detail partie).
- Ajouter un bouton copier/partager si absent.
- Ajouter/mettre a jour les tests backend et frontend.
- Avancement 2026-02-07 21:17: `CreateGameUseCase` corrige pour generer/persister `invitationCode` via `InvitationCodeService`.
- Avancement 2026-02-07 21:17: tests backend verts (`CreateGameUseCaseTest`, `GameDetailControllerTest`).
- Avancement 2026-02-07 21:47: feedback UI creation ajuste pour afficher immediatement le code d'invitation dans la snackbar si disponible.
- Avancement 2026-02-07 21:47: test frontend vert `create-game.component.spec.ts` (cas avec code d'invitation).
- Avancement 2026-02-07 22:11: `game-detail.component.html` corrige pour afficher le bloc invitation meme sans code si le createur peut en generer un (`*ngIf=\"game.invitationCode || canRegenerateCode()\"`).
- Avancement 2026-02-07 22:11: ajout fallback UI "pas de code" + bouton `generate-code-btn` qui declenche `promptRegenerateCode()`.
- Avancement 2026-02-07 22:11: tests frontend verts (`game-detail.component.spec.ts`, `join-game.component.spec.ts`, `game-detail.component.tdd.spec.ts`).
- Avancement 2026-02-07 22:52: `regenerateInvitationCode` frontend aligne sur backend (`duration` en query param) pour corriger les regenerations qui echouaient.
- Avancement 2026-02-07 22:52: `canRegenerateCode` autorise le createur meme hors `CREATING` pour coherence anciennes/nouvelles parties.
- Avancement 2026-02-07 22:52: UI invitation durcie (wrapper flex-wrap + libelle court `regenerateCode`) pour limiter les debordements.
- Avancement 2026-02-07 22:52: creation game affiche la cause d'erreur backend cote utilisateur (message explicite).
- Avancement 2026-02-07 23:31: quota creation actif corrige pour ignorer les games soft-deleted (`countByCreatorAndStatusInAndDeletedAtIsNull`).
- Avancement 2026-02-07 23:31: ajout handler `InvalidGameRequestException` dans `GlobalExceptionHandler` + test dedie (`GlobalExceptionHandlerTest`).
- Avancement 2026-02-07 23:31: tests backend verts `GameRepositoryAdapterTest,CreateGameUseCaseTest,GlobalExceptionHandlerTest` + test frontend vert `game-command.service.spec.ts`.
- Avancement 2026-02-07 23:37: remplacement du prompt texte de regeneration par une modale UX dediee (choix 24h/48h/7d/permanent) dans `game-detail`.
- Avancement 2026-02-07 23:37: nouvelles traductions ajoutees `fr/en/es/pt` pour la modale de duree.
- Avancement 2026-02-07 23:37: tests frontend game verts (322/322) + build frontend vert.
- Avancement 2026-02-08 11:32: harmonisation des actions `archive/quitter/supprimer/demarrer draft` vers la meme modale UX (`ConfirmDialogComponent`) dans `GameDetailActionsService`.
- Avancement 2026-02-08 11:32: specs converties de `window.confirm` vers `MatDialog.afterClosed()` dans `game-detail-actions.service.spec.ts`.
- Avancement 2026-02-08 11:32: validations vertes `game-detail-actions.service.spec.ts`, `features/game/**/*.spec.ts` (322/322), `npm run build`.
- Avancement 2026-02-08 11:40: retour utilisateur confirme que join-with-code fonctionne au moins depuis un point d'entree et que les traductions de l'ecran sont OK.
- Avancement 2026-02-08 11:40: regressions fonctionnelles remontees sur certaines games (code non regenerable, bouton supprimer absent), tickets dedies ouverts.
- Avancement 2026-02-08 11:54: ticket passe BLOCKED en attente de la correction owner/permissions `JIRA-FIX-007` pour valider la regeneration et les droits host sur toutes les games.

CRITERES_D_ACCEPTATION:
- [ ] Toute partie nouvellement creee dispose d'un code d'invitation.
- [ ] Le code est visible dans le detail de la partie pour le createur.
- [ ] Le code est copiable/partageable depuis l'interface.
- [ ] Tests cibles creation/query/affichage code sont verts.

NEXT:
- (1) Debloquer `JIRA-FIX-007` (creatorId/creatorName + permissions host coherentes)
- (2) Revalider create -> detail -> generer/copier code -> join with code sur games neuves et legacy
- (3) Clore le ticket puis le supprimer du backlog si tous les AC sont verifies


ID: JIRA-FIX-007
TITRE: Games "createur inconnu" et droits host incoherents
TYPE: Bug
PRIORITE: P1
STATUT: IN_PROGRESS
ESTIMATE: 4-8h

ASSIGNE: @Codex
ASSIGNE_LE: 2026-02-08 11:54
DERNIERE_MAJ: 2026-02-08 12:06

DEPENDENCIES: none

OBJECTIF:
- Identifier pourquoi certaines games apparaissent avec `createur inconnu`.
- Retablir des droits host coherents (supprimer/regenerer code) pour les games valides.

CONTEXTE:
- Retour utilisateur: des games visibles sur le profil Thibaut affichent `createur inconnu`.
- Sur ces games, les actions host disparaissent et seul "rejoindre" reste disponible.
- Suspicions: donnees legacy/orphelines, mapping incomplet `creatorId/creatorName`, ou regle permissions trop stricte.

DESCRIPTION:
- Tracer les payloads API (`creatorId`, `creatorName`, `status`, flags permissions) pour les games problematiques.
- Verifier en base les enregistrements orphelins (creator absent/null) et leur traitement applicatif.
- Definir une regle explicite pour games orphelines: correction data, fallback lisible, et blocage propre des actions host.
- Corriger la logique backend/frontend pour ne plus avoir de faux etats "joinable only" pour le createur legitime.
- Ajouter des tests backend + frontend sur cas nominal, cas legacy/orphelin, et permissions.
- Avancement 2026-02-08 11:54: `GameEntityMapper.toDomain` complete maintenant les games legacy sans participant createur en injectant un participant createur synthetique (id+username).
- Avancement 2026-02-08 11:54: `GameDtoMapper.fromDomainGame` resolve `creatorUsername/creatorName` via `creatorId` meme si le flag `isCreator` est absent.
- Avancement 2026-02-08 11:54: `GameApiMapper` mappe `creatorId` et ajoute un fallback `participants[creatorId]` avant `createur inconnu`.
- Avancement 2026-02-08 11:54: tests verts backend (`GameEntityMapperTest`, `GameDtoMapperTest`) et frontend (`game-api.mapper.spec.ts`).
- Avancement 2026-02-08 12:06: `GameDetailPermissionsService` aligne la detection host sur `userId` d'abord, puis fallback `username` (cas legacy), pour stabiliser `canDeleteGame` et `canRegenerateCode`.
- Avancement 2026-02-08 12:06: specs permissions mises a jour avec cas de priorite `userId` et fallback `username` + validation complete `features/game/**/*.spec.ts` verte (324/324).

CRITERES_D_ACCEPTATION:
- [ ] Aucune game active n'affiche `createur inconnu` sans raison explicite.
- [ ] Les droits host (supprimer/regenerer) sont coherents avec `creatorId`.
- [ ] Les games orphelines ont un comportement documente et teste.

NEXT:
- (1) Capturer payload reel `/api/games` et `/api/games/{id}` sur une game legacy pour verifier `creatorId/creatorName`
- (2) Validation manuelle Thibaut/Teddy: visibilite `supprimer/regenerer` sur game legacy + game neuve
- (3) Si OK, cloturer `JIRA-FIX-007` puis debloquer `JIRA-FIX-004`, `JIRA-FIX-008`, `JIRA-FIX-009`


ID: JIRA-FIX-008
TITRE: Bouton supprimer game absent ou incoherent selon le contexte
TYPE: Bug
PRIORITE: P1
STATUT: TODO
ESTIMATE: 2-4h

ASSIGNE: ->
ASSIGNE_LE: ->
DERNIERE_MAJ: ->

DEPENDENCIES: JIRA-FIX-007

OBJECTIF:
- Restaurer la visibilite et le comportement du bouton supprimer pour le createur quand l'action est autorisee.

CONTEXTE:
- Retour utilisateur: le bouton supprimer etait visible puis a disparu sur plusieurs games.
- L'incoherence apparait selon la game et le contexte d'affichage.
- Le bouton est critique pour nettoyer les games de test.

DESCRIPTION:
- Auditer `GameDetailPermissionsService` et `game-detail.component` pour les conditions `canDeleteGame`.
- S'assurer que la regle d'affichage est stable et alignee avec le backend (creator + statut + soft delete).
- En cas d'interdiction, afficher un etat explicite (disabled + raison) plutot qu'une disparition silencieuse.
- Ajouter des tests de non-regression (host autorise, host non autorise, non-host).

CRITERES_D_ACCEPTATION:
- [ ] Le createur voit toujours l'action supprimer quand les preconditions sont remplies.
- [ ] Les non-createurs ne voient pas l'action supprimer.
- [ ] Les cas non supprimables sont expliques de facon explicite dans l'UI.

NEXT:
- (1) Cartographier les conditions actuelles `canDeleteGame` front + backend
- (2) Corriger regle d'affichage et fallback UX
- (3) Valider manuellement + specs game-detail/permissions


ID: JIRA-FIX-009
TITRE: Regeneration code invitation incoherente selon les games
TYPE: Bug
PRIORITE: P1
STATUT: TODO
ESTIMATE: 2-4h

ASSIGNE: ->
ASSIGNE_LE: ->
DERNIERE_MAJ: ->

DEPENDENCIES: JIRA-FIX-007

OBJECTIF:
- Garantir que le createur peut regenerer un code invitation de facon fiable sur toutes les games eligibles.

CONTEXTE:
- Retour utilisateur: certaines games permettent la regeneration, d'autres non, sans logique visible.
- Impact direct sur l'invitation de nouveaux joueurs.
- Peut etre lie aux memes causes ownership que `createur inconnu`.

DESCRIPTION:
- Auditer les conditions d'affichage `canRegenerateCode` et les flags backend associes.
- Verifier la disponibilite endpoint de regeneration selon `status`, `creatorId`, `deletedAt`.
- Uniformiser regles front/back et afficher un message explicite quand l'action est refusee.
- Ajouter tests frontend/backend pour couvrir games neuves, games legacy, et statuts limites.

CRITERES_D_ACCEPTATION:
- [ ] Le createur peut regenerer sur toutes les games eligibles.
- [ ] En cas de refus, la raison est explicite pour l'utilisateur.
- [ ] Tests de regression regeneration verts.

NEXT:
- (1) Reproduire sur les games qui ne proposent pas la regeneration
- (2) Aligner les regles front/back de disponibilite du bouton et de l'endpoint
- (3) Revalider createur Thibaut sur plusieurs games


ID: JIRA-FIX-006
TITRE: Audit filtrage soft delete dans les requetes
TYPE: Task
PRIORITE: P2
STATUT: TODO
ESTIMATE: 1-2j

ASSIGNE: ->
ASSIGNE_LE: ->
DERNIERE_MAJ: ->

DEPENDENCIES: none

OBJECTIF:
- Verifier que les parties supprimees en soft delete sont correctement filtrees dans toutes les requetes.
- Auditer les usages de hard delete et migrer vers soft delete si necessaire.

CONTEXTE:
- Exigence produit: conserver l'historique en base, pas de suppression physique.
- Risque de suppressions physiques residuelles dans services/repositories.
- Risque que les requetes de listing ne filtrent pas le champ soft delete.

DESCRIPTION:
- Scanner services/repositories pour operations `delete` sur Game et agregats associes.
- Classer les cas: acceptable (maintenance) vs non conforme produit.
- Corriger les cas non conformes vers soft delete.
- Verifier que toutes les requetes de listing filtrent les enregistrements soft-deleted.
- Etendre l'audit aux autres domaines (Player, Team, Draft, Trade) si pertinent.

CRITERES_D_ACCEPTATION:
- [ ] Inventaire des suppressions hard delete produit.
- [ ] Cas non conformes corriges en soft delete.
- [ ] Requetes de listing filtrent correctement les soft-deleted.
- [ ] Tests cibles suppression + listing passent.

NEXT:
- (1) Scanner usages `delete` sur domaine game et autres domaines
- (2) Corriger flux non conformes vers soft delete
- (3) Verifier filtrage dans les requetes de listing


ID: JIRA-QUAL-001
TITRE: Migration consumers vers domain ports (services -> domain models)
TYPE: Story
PRIORITE: P3
STATUT: TODO
ESTIMATE: multi-session

ASSIGNE: ->
ASSIGNE_LE: ->
DERNIERE_MAJ: ->

DEPENDENCIES: none

OBJECTIF:
- Migrer progressivement les services applicatifs pour utiliser les domain ports au lieu des JPA repositories directs.
- Reduire le couplage entre couche service et couche persistence.

CONTEXTE:
- 5 domaines ont des pure domain models + ports + adapters (Game, Player, Team, Draft, Trade).
- Les services (TradingService, TradeQueryService, TeamService, etc.) utilisent encore les JPA repositories et model entities directement.
- La migration est incrementale : chaque service peut etre migre independamment.
- Pas bloquant pour la prod. Amelioration structurelle progressive.

DESCRIPTION:
- Ticket parent de migration incremental par service.
- `JIRA-QUAL-002` est traite hors perimetre Codex (autre IA).
- `JIRA-CLEAN-016` termine par Codex (refactor TradingService + tests verts).
- `JIRA-QUAL-003` termine par Codex: TeamService migre vers TeamDomainRepositoryPort/PlayerDomainRepositoryPort + tests TeamService verts.
- `JIRA-QUAL-004` termine et nettoye du backlog: sweep imports model effectue (118 imports, classification runtime vs bootstrap).
- `JIRA-QUAL-005` termine par @Claude: GameParticipantService deja sur ports domain (verification + tests verts).
- `JIRA-QUAL-006` termine par @Claude: GameDraftService migre vers DraftRepositoryPort/DraftPickRepositoryPort/PlayerRepositoryPort/GameParticipantRepositoryPort.
- `JIRA-QUAL-007` termine par Codex: TradeQueryService migre vers TradeDomainRepositoryPort avec bridge compat legacy, tests verts.
- `JIRA-QUAL-009` cree pour migration full-domain `TradeQueryUseCase + TradeController + TradeResponseDto`.
- Priorite temporairement abaissee derriere les tickets P1 issus du report utilisateur creation/rejoindre/suppression.

CRITERES_D_ACCEPTATION:
- [ ] Au moins 1 service migre vers domain ports par sous-ticket.
- [ ] 0 regressions backend par migration.
- [ ] Tests unitaires mis a jour pour chaque service migre.

NEXT:
- (1) Traiter d'abord les tickets JIRA-FIX P1 (bloquants UX)
- (2) Reprendre JIRA-QUAL-009 puis JIRA-QUAL-008


ID: JIRA-QUAL-008
TITRE: Sweep final imports model.* apres migrations runtime
TYPE: Task
PRIORITE: P3
STATUT: TODO
ESTIMATE: 2-4h

ASSIGNE: ->
ASSIGNE_LE: ->
DERNIERE_MAJ: ->

DEPENDENCIES: JIRA-QUAL-009

OBJECTIF:
- Finaliser la reduction des usages `com.fortnite.pronos.model.*` dans services/controleurs.
- Produire la liste finale des ecarts acceptes temporairement vs tickets correctifs restants.

CONTEXTE:
- Sweep intermediaire deja fait dans `JIRA-QUAL-004`.
- Il reste des zones runtime et des zones bootstrap/seed/auth a traiter explicitement.
- Ce ticket intervient apres migration trade full-domain (`JIRA-QUAL-009`).

DESCRIPTION:
- Re-scanner services et controllers.
- Classer en 3 groupes: `runtime corrige`, `runtime restant`, `bootstrap/seed/auth`.
- Ouvrir les derniers tickets READY necessaires avec dependances explicites.

CRITERES_D_ACCEPTATION:
- [ ] Inventaire final a jour.
- [ ] Classification priorisee finalisee.
- [ ] Backlog restant compose uniquement de tickets READY actionnables.

NEXT:
- (1) Re-executer le scan imports `model.*` sur services/controllers
- (2) Comparer avec baseline (118 imports) et mesurer delta
- (3) Creer les tickets correctifs restants ou clore parent si fini


ID: JIRA-QUAL-009
TITRE: Migration full-domain TradeQueryUseCase + TradeController + TradeResponseDto
TYPE: Story
PRIORITE: P2
STATUT: TODO
ESTIMATE: 1-2j

ASSIGNE: ->
ASSIGNE_LE: ->
DERNIERE_MAJ: ->

DEPENDENCIES: none

OBJECTIF:
- Supprimer le bridge compat temporaire trade query et migrer le flux read trade en full-domain.
- Conserver l'API REST publique stable (payload fonctionnellement equivalent).

CONTEXTE:
- `JIRA-QUAL-007` a migre `TradeQueryService` vers `TradeDomainRepositoryPort` avec bridge legacy.
- `TradeQueryUseCase` et `TradeController` retournent encore `model.Trade`.
- `TradeResponseDto` depend de `model.Trade` et `model.Player`.

DESCRIPTION:
- Migrer `TradeQueryUseCase` pour retourner `domain.trade.model.Trade`.
- Adapter `TradeController` pour consommer le modele domain.
- Introduire un mapper DTO dedie trade-domain -> `TradeResponseDto`.
- Adapter `TradeResponseDto` (ou mapper associe) pour supprimer dependance directe `model.Trade`.

CRITERES_D_ACCEPTATION:
- [ ] `TradeQueryUseCase` n'expose plus `model.Trade`.
- [ ] `TradeController` fonctionne sur modele domain sans regression API.
- [ ] `TradeResponseDto` mappe depuis domain via mapper dedie.
- [ ] Tests cibles + architecture passent.

NEXT:
- (1) Migrer interface `TradeQueryUseCase` et `TradeQueryService` vers retour domain
- (2) Adapter `TradeController` + mapping `TradeResponseDto`
- (3) Executer `mvn "-Dtest=TradeControllerTest,TradeQueryServiceTddTest,HexagonalArchitectureTest" test`
